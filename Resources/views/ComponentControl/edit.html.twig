{% from 'BtnAdminBundle:_macros:link.html.twig' import link_back, link_delete %}
{% extends 'BtnAdminBundle::base.html.twig' %}

{% block content %}

    {% set legend = 'btn_webplatform.component.edit'|trans({'%component_title%': component.title ?: component.type|trans, '%container_title%' : container.title}) %}
    {% include 'BtnWebplatformBundle:ComponentControl:_form.html.twig' with {form: form, legend: legend} only %}

    {{ link_back('btn_webplatform_componentcontrol_list', {container: component.container}) }}

    {% if container.manageable %}
        {{ link_delete('btn_webplatform_componentcontrol_delete', {id: component.id}) }}
    {% endif %}

{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/color.css') }}">
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/color.js') }}"></script>
    <script src="{{ asset('js/embeded.js') }}"></script>
{% endblock %}
